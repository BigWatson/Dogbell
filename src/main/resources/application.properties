spring.application.name=demo

# encryption.key is read from the ENCRYPTION_KEY environment variable.
# Example (PowerShell):
#   $env:ENCRYPTION_KEY = 'BASE64_32_BYTE_KEY'
# Or pass on the JVM command line: -Dencryption.key=BASE64_32_BYTE_KEY

#$env:ENCRYPTION_KEY = 'your-base64-32-byte-key-here'
#./gradlew bootRun

 # public broker for quick tests (no auth)
mqtt.broker=tcp://broker.hivemq.com:1883
# OR use local Mosquitto: tcp://localhost:1883
mqtt.client-id=demo-backend
textbelt.url=http://localhost:8080
textbelt.key=textbelt

# SMTP fallback for email->SMS gateway (when Textbelt unavailable)
# Example: Gmail (enable "App Passwords" in Security settings)
#   spring.mail.host=smtp.gmail.com
#   spring.mail.port=587
#   spring.mail.username=your-email@gmail.com
#   spring.mail.password=your-app-password
#   spring.mail.properties.mail.smtp.auth=true
#   spring.mail.properties.mail.smtp.starttls.enable=true
# Or your SMTP server:
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=alfred.sherlock@gmail.com
spring.mail.password=gcypapzejhxywgte
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

sms.from=alfred.sherlock@gmail.com

#Maybe remove this
server.port=8081
server.servlet.context-path=/
logging.level.root=INFO
logging.level.com.example=DEBUG